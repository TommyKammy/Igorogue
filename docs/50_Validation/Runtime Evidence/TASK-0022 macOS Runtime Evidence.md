---
type: validation-report
id: VALIDATION-TASK-0022-MACOS
status: review
project: Igorogue
task: TASK-0022
evidence_level: runtime-bootstrap
updated: 2026-07-11
---
# TASK-0022 macOS Runtime Evidence

## Verdict

The pinned bootstrap succeeds both in the task worktree and from a detached clean worktree at implementation commit `c1e1998d34f7e9abbb8962b7cc34897ebd9675a1`. Authentic lock files are committed at `70f0eec`. Local and clean-checkout gates pass. CI passed on the previous PR head, but the final review-correction commit has not run yet; Stage 4 therefore remains pending.

## Identity

| Field | Value |
|---|---|
| Handoff version | v0.2.10 |
| Runtime implementation commit | `c1e1998d34f7e9abbb8962b7cc34897ebd9675a1` |
| Lock-file commit | `70f0eec` |
| Content schema | 1 |
| Content hash | `sha256:b411ddf2dfb8e876370d11f2259368b7d898fcfebe8a4e4fb24c30802968ee06` |
| Host | macOS 26.5.1 (25F80), arm64 |
| .NET SDK | 8.0.422, arm64 |
| Godot | 4.7.stable.mono.official.5b4e0cb0f |
| Renderer | Compatibility |

Private host paths are represented as `<REPO_ROOT>`, `<CLEAN_WORKTREE>`, and `<GODOT_BIN>` in committed evidence.

## Lock generation

- All eight expected `packages.lock.json` files were generated by .NET SDK 8.0.422.
- A second `tools/dev/update-locks` run produced the same file set and no lock diff.
- No central package version or NuGet source changed.
- Godot export-specific restore graphs are isolated under ignored `build/nuget-locks/`; the eight committed development locks remain unchanged after export.

## Local and clean-checkout results

| Gate | Task worktree | Detached clean worktree |
|---|---:|---:|
| Repository governance | pass | pass |
| Exact tool verification | pass | pass |
| Locked restore | pass | pass |
| Release build | pass, 0 warnings / 0 errors | pass, 0 warnings / 0 errors |
| xUnit | pass, 11/11 | pass, 11/11 |
| Simulator repeat | matching | matching |
| Godot .NET headless smoke | pass | pass |
| Windows x86_64 debug export | pass | pass |
| Managed export assembly | present | present |
| SHA-256 sidecar | present | present |
| Tracked-file cleanliness | clean | clean |

The latest completed CI before the final review corrections was run [`29127755203`](https://github.com/TommyKammy/Igorogue/actions/runs/29127755203) at `794588c8a643e670b7b49deb212bc5e6171e9e9e`:

- Governance and generated content — pass, including a real PowerShell native exit-17 fail-fast probe;
- Pure .NET build, tests, and simulator smoke — pass;
- Godot .NET headless smoke and complete Windows debug export — pass.

Simulator and Godot smoke both emitted:

```text
checksum=3b59c2c2c2f20ec64af8a325a38ea48e7647935fa4a90c06ce2251e49879bcdd
content=sha256:b411ddf2dfb8e876370d11f2259368b7d898fcfebe8a4e4fb24c30802968ee06
files=7
```

The clean-checkout Windows export contained `Igorogue.exe`, `Igorogue.console.exe`, `Igorogue.exe.sha256`, and `data_Igorogue.Godot_windows_x86_64/Igorogue.Godot.dll`. Its executable SHA-256 was `0aeded8aaf1b7398549906215aa5ec1cbc16262055b3dba555d036b69fe71d5a`.

## Bootstrap defect corrected

The initial export command could return zero while Godot reported a failed managed publish, leaving only a native executable. The bounded correction:

- adds the Godot-local solution and generated C# UID required by Godot 4.7;
- verifies the native executable, managed data directory, and `Igorogue.Godot.dll`;
- archives the complete Windows export instead of the executable alone;
- separates Godot platform-specific restore locks from committed development locks;
- fails wrappers and CI if export rewrites a committed lock file.

No gameplay code, package version, version pin, accepted rule, scene, resource, project setting, or export preset changed.

## Detailed records

- [[TASK-0022-command-log.txt]]
- `data/TASK-0022-artifact-hashes.json`

## Remaining gate

Stage 4 is pending for the commit that contains the final review corrections. The associated GitHub check on that commit is the authoritative final-CI evidence; this committed file does not claim success for an unexecuted future run. The report remains `review` until that check is green and a human makes the `review → done` and merge decisions.

The Windows debug export is integrity-hashed but bit-for-bit reproducible export output is not a TASK-0022 acceptance criterion. Separate local and clean-worktree exports produced different executable hashes; runtime state determinism continues to be covered by the identical simulator and Godot smoke checksums.
