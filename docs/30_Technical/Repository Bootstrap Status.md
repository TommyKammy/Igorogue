---
type: technical-status
id: TECH-BOOTSTRAP-0001
status: review
project: Igorogue
updated: 2026-07-10
---
# Repository Bootstrap Status

## Purpose

Record which parts of [[TASK-0001 Decide Engine and Repository]] are present in the repository and which still require execution on a configured host.

## Implemented repository artifacts

- `Igorogue.sln`
- exact `.NET SDK 8.0.422` pin in `global.json`
- Central Package Management for xUnit v3
- pure `net8.0` Domain, Application, and Content projects
- formal simulator CLI using Application and Content
- Godot 4.7 .NET presentation project and smoke scene
- architecture tests that prohibit Domain/Application references to Godot
- deterministic content snapshot generator
- exact tool verifier
- POSIX and PowerShell wrappers
- GitHub Actions governance, .NET, Godot smoke, and Windows export jobs

## Evidence available in this package

Evidence level: **static bootstrap evidence**.

The repository structure, XML/JSON syntax, dependency direction, wrapper parity, content generation, and tool-verifier behavior are tested by Python checks. Generated runtime content is committed and compared byte-for-byte with `game_data/`.

## Evidence still required

The packaging environment used to create this archive did not contain Godot or the .NET SDK. Therefore these acceptance criteria remain unproven until run on the development host or CI:

1. `dotnet restore --locked-mode`
2. `dotnet build`
3. xUnit execution
4. formal simulator smoke execution
5. Godot C# headless build
6. Godot bootstrap scene exit code 0
7. Windows debug export and artifact hash
8. committed `packages.lock.json` files generated by the pinned SDK

Run `tools/dev/update-locks` once on a configured host, review and commit every lock file, then run the full evidence sequence in [[TASK-0001 Decide Engine and Repository]].

## Tool contract

```text
.NET SDK: 8.0.422 exactly
Godot: 4.7-stable .NET/Mono editor exactly
Godot renderer: Compatibility
C#: 12.0
Target framework: net8.0
Test runner package: xunit.v3.mtp-v1 3.2.2
```

A standard non-.NET Godot editor is rejected.

## macOS handoff execution

Use [[TASK-0022 Bootstrap macOS Host and Close Runtime Evidence]] and [[macOS Development Host Setup]]. Do not start gameplay Rules Kernel work until the task closes the runtime and CI evidence gap.
