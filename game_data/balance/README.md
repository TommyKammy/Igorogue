# Balance Data

現在値の正本候補です。設計意図と健全範囲はObsidianへ、実行時の値はここへ置きます。v0.2実装前の候補値であり、製品確定値ではありません。

`system.json`の`coordinate_system`と`initial_position`はSPEC-COORD-001に対応する実行時候補値です。外部データはCanonicalPoint `[x,y]`（1-based）を使用します。
