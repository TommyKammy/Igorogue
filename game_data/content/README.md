# Content Data

このdirectoryは現在のruntime content値の正本である。カード定義は`cards.json`、Core Duel starting recipeは`starting_decks.json`を使用し、対応schemaは`docs/30_Technical/Schemas/`に置く。`build/generated_content/`と`game/Igorogue.Godot/generated_content/`はcontent syncで生成し、直接編集しない。表示名は将来ローカライズキーへ置換する。
