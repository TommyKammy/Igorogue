#!/usr/bin/env sh
set -eu
. "$(dirname "$0")/_common.sh"
"$(dirname "$0")/godot-smoke"
GODOT_BIN=${GODOT_BIN:?Set GODOT_BIN to the Godot 4.7 stable .NET executable.}
mkdir -p build/exports/windows
"$GODOT_BIN" --headless --path game/Igorogue.Godot --export-debug "Windows Debug" "$ROOT/build/exports/windows/Igorogue.exe"
"$PYTHON_BIN" tools/hash_artifact.py build/exports/windows/Igorogue.exe
