#!/usr/bin/env sh
set -eu
. "$(dirname "$0")/_common.sh"
"$PYTHON_BIN" tools/verify_toolchain.py --skip-godot
"$PYTHON_BIN" tools/content_sync.py --write
"$DOTNET_BIN" restore Igorogue.sln --use-lock-file --force-evaluate
"$PYTHON_BIN" tools/check_repository_bootstrap.py --strict-locks
printf '%s\n' 'Lock files generated. Review and commit every packages.lock.json before using locked restore.'
